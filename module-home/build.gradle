apply from: '../build.module.gradle'

android {
    defaultConfig {

        if (isBuildModule.toBoolean()) {
            applicationId rootProject.ext.modules['package_module_home']
        }
    }

    resourcePrefix rootProject.ext.resourcePrefix['package_module_home']
}

dependencies {


}