apply from: '../module.build.gradle'

android {
    defaultConfig {

        if (isBuildModule.toBoolean()) {
            applicationId 'com.yazao.home'
        }
    }

    resourcePrefix 'home_'
}

dependencies {


}