apply from: '../module.build.gradle'

android {
    defaultConfig {

        if (isBuildModule.toBoolean()) {
            applicationId rootProject.ext.modules['package_module_login']
        }
    }

    resourcePrefix rootProject.ext.resourcePrefix['package_module_login']
}

dependencies {


}